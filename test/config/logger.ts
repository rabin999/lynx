import config from "../../app/config"
import LoggerConfigInterface from "../../app/config/interface/logging";

describe("Configuration Test", () => {
  it("should return hello world", () => {
    expect(5).toEqual(5);
  });
});
