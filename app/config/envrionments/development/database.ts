import DatabaseConfigInterface from "../../interface/database"

export default <DatabaseConfigInterface> {
    host: "localhost",
    port: 3306,
    user: "employee",
    password: "password@1",
    database: 'employees',
    debug: true
}